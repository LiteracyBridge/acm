Improvements:
    * cache user info; fallback to cache when no network/timeout

Features:
    * Changing program

Configuration:
    * create new aws user pool app id
    * 253ghc

Bugs:
    * changing program should download/update content from s3 before navigating to home screen
    * maintain folder structure after unzipping content - NO RESTRUCTURING (doesn't work always)